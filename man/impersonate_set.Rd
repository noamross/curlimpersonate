% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{impersonate_set}
\alias{impersonate_set}
\title{Set the active impersonation profile}
\usage{
impersonate_set(profile, headers = TRUE)
}
\arguments{
\item{profile}{A target string (see \code{\link[=impersonate_profiles]{impersonate_profiles()}}), e.g.
\code{"chrome131"}.}

\item{headers}{Whether libcurl-impersonate should also supply the browser's
default headers and ordering (\code{CURL_IMPERSONATE_HEADERS}). See the Headers
section.}
}
\value{
\code{profile} invisibly.
}
\description{
libcurl-impersonate reads the \code{CURL_IMPERSONATE} environment variable each
time a request handle is initialised, so this changes the TLS/HTTP2
fingerprint for all subsequent requests made through the \code{curl} package
(and \code{httr}/\code{httr2}) in this session. It takes effect only when
libcurl-impersonate is actually in use (via \code{\link[=activate]{activate()}} or the preload
mechanism); on a stock \code{curl} package it does nothing.
}
\details{
Profiles available in this package's bundled list:
chrome100, chrome101, chrome104, chrome107, chrome110, chrome116, chrome119, chrome120, chrome123, chrome124, chrome131, chrome99, chrome99_android, edge101, edge99, firefox117, firefox133, safari15_3, safari15_5, safari17_0, safari17_2_ios, safari18_0.
This list is informational and not exhaustive — any target your
libcurl-impersonate build supports will work even if it is not listed.
}
\section{Headers}{

libcurl-impersonate can do two things: match the browser's \strong{TLS/HTTP2
fingerprint}, and (optionally) send the browser's \strong{default request headers}
in the browser's order. The \code{headers} argument maps to the
\code{CURL_IMPERSONATE_HEADERS} environment variable:
\itemize{
\item \code{headers = TRUE} (default): the library installs the browser's default
headers (e.g. \code{User-Agent}, \code{Accept}, \code{Accept-Language}, \code{sec-ch-ua}, ...)
in the correct order. This is usually what you want, since header set and
order are themselves fingerprinted.
\item \code{headers = FALSE}: only the TLS/HTTP2 fingerprint is impersonated; you are
fully responsible for headers.
}

\strong{Interaction with headers you set yourself} (via \code{httr2::req_headers()},
\code{httr::add_headers()}, or \code{curl::handle_setopt(.list = list(httpheader =))}):
all of these set libcurl's \code{CURLOPT_HTTPHEADER}, which is \emph{replace}, not
merge. Headers you set generally \strong{override} the impersonated defaults for
the fields you specify and can disturb header ordering, which may make the
request look less like the target browser. If you only need to add or change
a header (e.g. \code{Authorization}), expect that it may move to the end / replace
the browser default. When fingerprint fidelity matters, set as few headers as
possible yourself, and verify the result with \code{\link[=impersonate_check]{impersonate_check()}}. (These
are the expected mechanics of libcurl-impersonate; confirm against your build.)
}


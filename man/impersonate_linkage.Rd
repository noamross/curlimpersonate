% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preload.R
\name{impersonate_linkage}
\alias{impersonate_linkage}
\title{Inspect how the \code{curl} package links libcurl}
\usage{
impersonate_linkage(so = .active_curl_so())
}
\arguments{
\item{so}{Path to a \code{curl.so}/\code{curl.dll} to inspect. Defaults to the \code{curl}
package on the current search path.}
}
\value{
An \code{impersonate_linkage} object: the inspected object, \code{linkage}
(\code{"dynamic"}, \code{"static"}, or \code{NA} on Windows), the dynamic libcurl
dependency if any, and whether it already points at libcurl-impersonate.
}
\description{
Examines the compiled object of the \code{curl} \emph{package} that would currently be
loaded and reports whether it links libcurl \strong{statically} or \strong{dynamically}
— which determines whether you can use the preload mechanism
(\code{\link[=impersonate_env]{impersonate_env()}}) or must rebuild (\code{\link[=build_impersonate_curl]{build_impersonate_curl()}}).
}
\examples{
# Inspect how the curl package links libcurl to choose the right mechanism
lnk <- impersonate_linkage()
lnk
# "dynamic" -> use impersonate_env() (no rebuild needed)
# "static"  -> use build_impersonate_curl()
}

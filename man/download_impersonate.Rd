% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_impersonate}
\alias{download_impersonate}
\title{Download a prebuilt libcurl-impersonate into the managed cache}
\usage{
download_impersonate(
  version = "latest",
  repo = getOption("curlimpersonate.repo", "lexiforest/curl-impersonate"),
  os = .detect_os(),
  arch = .detect_arch(),
  quiet = FALSE
)
}
\arguments{
\item{version}{Release tag (e.g. \code{"v1.0.0"}) or \code{"latest"}.}

\item{repo}{GitHub \code{owner/repo} publishing the prebuilt binaries. Defaults
to the actively maintained \code{lexiforest/curl-impersonate} fork; override
with the \code{curlimpersonate.repo} option or this argument.}

\item{os, arch}{Platform identifiers; auto-detected by default.}

\item{quiet}{Suppress progress messages.}
}
\value{
The directory the library was written to (invisibly).
}
\description{
Fetches a prebuilt release archive for the current OS/architecture and
unpacks the shared library (and headers, if bundled) into
\code{tools::R_user_dir("curlimpersonate", "data")}. Skip this entirely by
setting \code{CURLIMPERSONATE_HOME} to an existing install.
}
\examples{
\dontrun{
  download_impersonate()                    # latest release for your OS/arch
  download_impersonate(version = "v0.8.0")  # pin a specific release tag

  # Skip downloading by pointing at an existing install instead:
  # Sys.setenv(CURLIMPERSONATE_HOME = "/opt/curl-impersonate")
}
}

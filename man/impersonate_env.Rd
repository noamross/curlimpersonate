% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preload.R
\name{impersonate_env}
\alias{impersonate_env}
\title{Environment variables for the preload (no-rebuild) mechanism}
\usage{
impersonate_env(profile = NULL, lib = impersonate_lib())
}
\arguments{
\item{profile}{Optional impersonation target to include as \code{CURL_IMPERSONATE}.
Unlike the loader variables, \code{CURL_IMPERSONATE} can also be set at runtime
with \code{\link[=impersonate_set]{impersonate_set()}}.}

\item{lib}{Path to the libcurl-impersonate library. Defaults to
\code{\link[=impersonate_lib]{impersonate_lib()}}.}
}
\value{
A named character vector of environment variables, with class
\code{impersonate_env} for pretty printing.
}
\description{
When the \code{curl} package links libcurl \strong{dynamically}, you can make the
\emph{existing} \code{curl} package use libcurl-impersonate by preloading it — no
rebuild needed. These loader variables must be present \strong{before R starts},
because the dynamic linker reads them at process launch. Putting them in
\code{.Renviron} is generally \strong{too late}; set them in your shell, a launcher
script, or a wrapper.
}
\details{
On macOS the CRAN \code{curl} package is statically linked, so preloading cannot
redirect it (it \emph{can} affect \code{\link[=download.file]{download.file()}} with \code{method = "libcurl"},
which uses a dynamically linked libcurl). \code{\link[=impersonate_env]{impersonate_env()}} warns when it
detects this.
}
\examples{
\dontrun{
  # After download_impersonate() on a system where curl links libcurl dynamically:
  impersonate_env("chrome131")
  # Copy the printed export lines into your shell/launcher, then restart R
}
}

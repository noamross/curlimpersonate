% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/activate.R
\name{activate}
\alias{activate}
\title{Put the rebuilt curl package first on the library search path}
\usage{
activate()
}
\value{
The activated library path (invisibly).
}
\description{
Prepends the private library (where \code{\link[=build_impersonate_curl]{build_impersonate_curl()}} installed the
impersonate-linked \code{curl}) to \code{\link[=.libPaths]{.libPaths()}}, so it loads ahead of the system
\code{curl}. All downstream users of the \code{curl} package (\code{curl} itself, \code{httr},
\code{httr2}, ...) then use libcurl-impersonate.
}
\details{
This only does the obvious thing — it edits \code{.libPaths()} for the current
session. It must run \strong{before \code{curl} is loaded}; once \code{curl} is loaded its
compiled object cannot be swapped. So either call it at the very top of your
script before any package that uses \code{curl}, or arrange it at startup
yourself, e.g. in \code{.Rprofile}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{if (requireNamespace("curlimpersonate", quietly = TRUE)) curlimpersonate::activate()
}\if{html}{\out{</div>}}

or, equivalently and without loading the package, prepend the library via
\code{R_LIBS} in \code{.Renviron} (run \code{cat(curlimpersonate:::.rlib_dir())} to get the
path):

\if{html}{\out{<div class="sourceCode">}}\preformatted{R_LIBS=/path/to/curlimpersonate/rlib
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
  # Call activate() before any library() that loads the curl package:
  activate()
  library(httr2)
  impersonate_set("chrome131")
  chk <- impersonate_check()  # JA4 should now match Chrome's fingerprint
  chk$ja4
}
}
